This is the BOTTOM-RIGHT QUADRANT of a printed A0 poster. Upscale to razor-crisp 4K. KEEP THE EXACT crop, composition, colours and all four cut edges identical so it tiles seamlessly. Do NOT move panels or change the layout. Pure white background; Salford crimson red, teal, navy accents unchanged.

CRITICAL: several small paragraphs are currently blurred into nonsense words and one chart axis is mislabelled. Re-render ALL text crisply and replace any blurry/garbled words with these EXACT short labels (use short chips and bullet fragments, NOT long sentences, so every word is sharp):

Panel "System Architecture":
  intro line: "Three coordinated GPU containers:"
  three chips: "gaussian-toolkit  -  CUDA 12.8" / "milo  -  CUDA 11.8" / "come  -  CUDA 12.1"
  one line: "LichtFeld Studio v0.5.2  -  USD I/O  -  MCP control (70+ tools)"
  hardware line (keep, already correct): "dual RTX 6000 Ada (96 GB VRAM), Threadripper PRO, 251 GB RAM"

Panel "Per-Object 360 Reconstruction" (left side): a left-to-right flow of four icon chips:
  "64 orbit views" -> "FLUX.1 inpainting" -> "Hunyuan3D 2.0 (ComfyUI)" -> "Watertight PBR mesh"

Panel "Deployment & Roadmap": "University of Salford funded trial" then "1. Pilot gallery capture & validation  2. Scale to multi-room collections  3. WebXR deliverable".

Scene-graph variant nodes (top-left of this quadrant): "Sculpture_03 -> variant {Gaussian | Mesh}".

Panel "Quantitative Outcome": four rows reading EXACTLY:
  "Coverage (SAM2 -> field)   98.3% of gaussians assigned"
  "Objects emplaced   31 prims at surveyed pose"
  "Registration   sub-cm against survey control"
  "Deliverable   18 MB .ksplat + USD scene graph"

Bar chart: title "Coverage (%)", x-axis label "Accuracy (cm)", bars at x = 0, 1, 5, 10, 25 (decreasing). NO other text on the chart.

REMOVE the crumpled red and teal fabric-like blobs entirely; if you place object thumbnails there, render clean small grey textured 3D sculpture busts on a white background. Absolutely NO invented, lorem-ipsum, or misspelled words anywhere in this quadrant. Output the refined quadrant only.
