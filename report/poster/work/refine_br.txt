This image is the BOTTOM-RIGHT QUADRANT of a printed A0 scientific poster. Upscale it to razor-crisp 4K for large-format print. Keep the EXACT same layout, composition, colours, icons and crop. Keep all four cropped edges precisely as they are (content cut off at an edge stays cut off in the same place) so the quadrant tiles seamlessly. Do NOT add borders, frames or padding. Pure white background; University of Salford crimson red, teal and navy accents unchanged.

Render ALL text crisp and spelled EXACTLY as follows (correct any blur to these exact words):
- Tail of the mesh-backends table (cut at top): two rows reading EXACTLY:
  "CoMe   compact, manifold   25 min"
  "GaussianWrap   thin structures   38 min"
- Panel heading "System Architecture" then body text reading EXACTLY:
  "Three coordinated GPU containers run the heterogeneous toolchain: gaussian-toolkit CUDA 12.8, milo CUDA 11.8, come CUDA 12.1."
  with a hardware line below reading EXACTLY:
  "dual RTX 6000 Ada (96 GB VRAM), Threadripper PRO, 251 GB RAM".
- Panel heading "Quantitative Outcome" then four rows reading EXACTLY:
  "Coverage (SAM2 -> field)   98.3% of gaussians assigned"
  "Objects emplaced   31 prims at surveyed pose"
  "Registration   sub-cm against survey control"
  "Deliverable   18 MB .ksplat + USD scene graph"
- A small bar chart titled "Coverage (%)" with x-axis label "Accuracy (cm)" and bars at 0, 1, 5, 10, 25.
IMPORTANT: replace any crumpled red or teal fabric-like blobs with clean small textured 3D sculpture thumbnails on white, or remove them. No lorem-ipsum or invented words anywhere.
Only increase fidelity: cleaner anti-aliased text, smoother flat-vector icons. Output the refined quadrant only.
